version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - misspell
    - unconvert
    - unparam
    - revive
  settings:
    revive:
      rules:
        - name: unused-parameter
          disabled: true

formatters:
  enable:
    - gofmt
    - goimports
